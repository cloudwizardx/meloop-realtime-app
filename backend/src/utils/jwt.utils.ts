// import jwt from 'jsonwebtoken'

// const PUBLIC_KEY_PATH = process.env.JWT_PUBLIC_KEY || 'public

// export const signAccessToken = (claims: ClaimsPayload): string => {
//     jwt.sign(claims, process.env.JWT)
// }